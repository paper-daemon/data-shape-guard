# Changelog

## Unreleased
- Redact examples for secret-like bracketed JSON paths, including keys such as `api key` and `auth token`.
- Add regression coverage proving non-secret bracketed keys remain visible while secret-like values are hidden.

## 1.0.0
- Initial public OSS release.
